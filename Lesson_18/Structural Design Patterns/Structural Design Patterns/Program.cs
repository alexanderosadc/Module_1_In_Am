using Structural_Design_Patterns.Decorator;
using System;

namespace Structural_Design_Patterns
{
    class Program
    {
        static void Main(string[] args)
        {
            /*EnemyProxy enemyProxy = new EnemyProxy(new Enemy());
            enemyProxy.DecreaseHealth(10);
            ISmallEnemy enemy = new SmallEnemy(10, 10);
           
            PowerUpDecorator powerUpDecorator = new PowerUpDecorator(enemy);
            PowerUpDecorator secondPowerUpDecorator = new PowerUpDecorator(powerUpDecorator);

            Console.WriteLine(secondPowerUpDecorator.GetAmountOfDamage());*/
        }
    }
}
